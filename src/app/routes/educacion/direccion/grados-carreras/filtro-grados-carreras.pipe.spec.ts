import { FiltroGradosCarrerasPipe } from './filtro-grados-carreras.pipe';

describe('FiltroGradosCarrerasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroGradosCarrerasPipe();
    expect(pipe).toBeTruthy();
  });
});
